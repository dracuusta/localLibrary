extends layout 

block content

    h1 #{title}


    form(method='POST')
        div.form-group 
            label(for='name') Genre: 
            input#name.form-control(type="text", placeholder='Fantasy, Poetry etc.')
