extends layout 

block content

    h1 #{title}: #{bookInstance.book.title} 
    p id: #{bookInstance._id}


    p Do you really want to delete this Book Instance?

    form(method='POST')
      div.form-group
        input#id.form-control(type='hidden', name='bookinstanceid', value=bookInstance._id )

      button.btn.btn-primary(type='submit') Delete
